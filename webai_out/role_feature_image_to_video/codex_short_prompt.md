Read `webai_out/role_feature_image_to_video/ROLE_OUTPUT_MANIFEST.json` first. Apply only a small imagevideo contract patch to the 7861 experiment copy. Preserve `ai_studio_inbox_imagevideo`, `ai_studio_inbox_prompt_imagevideo`, and modes `single/endframes`. Add UI slots/status/payload preview plus safe `/api/imagevideo/capabilities` and `/api/imagevideo/prepare` routes only. Do not enable real generation unless workflow slot mapping + preflight are explicit. Return changed files, API smoke summaries, console error count, and whether ComfyUI `/prompt` was submitted; expected false.
